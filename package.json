{
  "name": "sks",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
