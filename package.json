{
  "name": "sks",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "storybook": "lerna run storybook",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
